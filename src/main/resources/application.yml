server:
  servlet:
    context-path: /
spring:
  jackson:
    default-property-inclusion: non_null
  application:
    name: gerenciamento
  datasource:
    url: jdbc:h2:mem:test
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
app:
  pageSize: 3
